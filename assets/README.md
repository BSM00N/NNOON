# Assets Directory

이 디렉토리에 이미지와 폰트 파일을 저장합니다.

## 구조
```
assets/
├── images/
│   └── profile.jpg    # 프로필 사진 (권장: 300x300px)
└── fonts/
    └── (custom fonts)
```

## 프로필 사진 추가 방법
1. `assets/images/` 폴더에 `profile.jpg` 파일을 추가하세요
2. 권장 크기: 300x300px 이상의 정사각형 이미지
3. 지원 형식: JPG, PNG, WebP
